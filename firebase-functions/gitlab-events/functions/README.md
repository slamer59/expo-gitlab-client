# Setup notification system for gitalchemy application

## Setup env

`
 . "/home/thomas/Developpments/gitlab-client/expo-gitlab-client/firebase-functions/gitlab-events/functions/venv/bin/activate" && uv pip install -r requirements.txt`
 
## Command

`firebase emulators:start`

`firebase deploy --only functions`

